---
date: 2022-04-10
tags: ["Raspberry Pi", "Linux", "WLAN"]
description: "本文提供了解决树莓派4B上`wlan0`无线网络接口消失问题的两种快速方法。第一种是当接口未启动时，使用`sudo ifconfig wlan0 up`命令手动启用。第二种是当接口被软件锁定时，使用`sudo rfkill unblock all`命令进行解锁。"
---

# 树莓派4B wlan0消失



1.  wlan0 没有启动，使用`sudo ifconfig wlan0 up`启用即可
2.  wlan0被blocked，使用`sudo rfkill unblock all`解锁即可
